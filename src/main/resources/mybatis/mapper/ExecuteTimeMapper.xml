<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.ExecuteTimeDao">
    <insert id="insert">
        insert into execute_time(`name`,`total_time`,`io_time`,`io_detail`)
        values(#{name},#{totalTime},#{ioTime},#{ioDetail})
    </insert>
</mapper>